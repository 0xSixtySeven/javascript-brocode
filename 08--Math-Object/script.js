


let x = 3;
let y = 2;
let z = 1;

// z = Math.round(x); // Math.round is a method that rounds the number to the nearest integer.
// z = Math.ceil(x); // Math.ceil is a method that rounds the number up to the nearest integer.
// z = Math.floor(x); // Math.floor is a method that rounds the number down to the nearest integer.
// z = Math.trunc(x); // Math.trunc is a method that removes the decimal part of the number.
// z = Math.pow(x, y); // Math.pow is a method that returns the value of a number(x) raised to the power of another number(y).
// z = Math.sqrt(x); // Math.sqrt return the square root of (x).
// z = Math.log(x); // Math.log returns the logarithm of (x)
// z = Math.sin(x); // Math.sin returns the sine of (x)
// z = Math.cos(x); // Math.cos returns the cosine of (x)
// z = Math.tan(x); // Math.tan returns the tangent of (x)
// z = Math.abs(x); // Math.abs transforms a negative number to a positive one
// z = Math.sign(x); // Math.sign returns the sign of a number, indicating whether the number is positive(1), negative(-1) or zero(0).


// HOW TO GET THE HIGHEST NUMBER OF THREE VARIABLES USING Math.max
let max = Math.max(x, y, z); // Math.max returns the highest number of the arguments.
let min = Math.min(x, y, z); // Math.min returns the lowest number of the arguments.

console.log(min); 